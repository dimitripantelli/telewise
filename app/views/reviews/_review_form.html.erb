<%= simple_form_for [@show, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, input_html: { min: '0', step: '1' }%>
  <%= f.submit 'Add a review', class: "btn btn-primary" %>
<% end %>
